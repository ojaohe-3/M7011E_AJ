export class Manager{
    constructor(id : String, maxProduciton: number){
        this.id = id;
        this.current = 0;
        this.maxProduciton = maxProduciton;
        this.acceleration = 
        this.produciton = async (ratio:number) : number => {
            let speed = .0001;
            let curr = 0;
            while(curr )
        } 

    }
}